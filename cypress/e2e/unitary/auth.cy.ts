describe('Pruebas unitarias modulo de autenticacion', () => {
  it('Verificar funcionalidad del formulario del login', () => {
      cy.visit('/')
  })
  it('Verificar funcionalidad del los formulario de cambiar contrasena', () => {
      cy.visit('/')
  })
  it('Verificar funcionalidad del formulario de recuperar contrasena', () => {
      cy.visit('/')
  })
})