describe('Pruebas unitarias modulo de perfil', () => {
  it('Verificar funcionalidad del los componentes de formulario de datos', () => {
      cy.visit('/')
  })
  it('Verificar funcionalidad del los componentes del formulario para cambiar contrasena', () => {
      cy.visit('/')
  })
  it('Verificar funcionalidad de cerrar sesion', () => {
      cy.visit('/')
  })
})

