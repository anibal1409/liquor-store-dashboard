describe('Pruebas unitarias modulo de usuarios', () => {
  it('Verificar funcionalidad del los componentes de la lista', () => {
      cy.visit('/')
  })
  it('Verificar funcionalidad del los componentes del formulario', () => {
      cy.visit('/')
  })
  it('Verificar funcionalidad del los componentes del modal', () => {
      cy.visit('/')
  })
  it('Verificar funcionalidad del modal de confirmacion', () => {
      cy.visit('/')
  })
})
