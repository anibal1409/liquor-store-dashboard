describe('Pruebas unitarias modulo de estadisticas', () => {
  it('Verificar funcionalidad del los componentes de la lista', () => {
      cy.visit('/')
  })
  it('Verificar funcionalidad del los componentes de reportes', () => {
      cy.visit('/')
  })
})
